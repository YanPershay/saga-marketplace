trigger:
- main

pool:
  vmImage: ubuntu-latest

variables:
  buildConfiguration: Release
  acrServiceConnection: sagamarketplaceacr
  acrLoginServer: sagamarketplaceacr.azurecr.io
  imageTag: $(Build.BuildId)

steps:
- task: UseDotNet@2
  displayName: Use .NET SDK
  inputs:
    packageType: sdk
    version: 9.x

- script: dotnet restore
  displayName: Restore

- script: dotnet build --configuration $(buildConfiguration) --no-restore
  displayName: Build

- script: dotnet test --configuration $(buildConfiguration) --no-build
  displayName: Test

- task: Docker@2
  displayName: Build and push order-api image
  inputs:
    command: buildAndPush
    repository: order-api
    dockerfile: services/order/Order.API/Dockerfile
    buildContext: .
    containerRegistry: $(acrServiceConnection)
    tags: |
      $(imageTag)
      latest

- task: Kubernetes@1
  displayName: Apply order-api manifests
  inputs:
    connectionType: Kubernetes Service Connection
    kubernetesServiceEndpoint: saga-aks
    namespace: default
    command: apply
    arguments: -f infra/k8s/order-api/deployment.yaml -f infra/k8s/order-api/service.yaml

- task: Kubernetes@1
  displayName: Update order-api image
  inputs:
    connectionType: Kubernetes Service Connection
    kubernetesServiceEndpoint: saga-aks
    namespace: default
    command: set
    arguments: image deployment/order-api order-api=sagamarketplaceacr.azurecr.io/order-api:$(imageTag)

- task: Kubernetes@1
  displayName: Check order-api rollout status
  inputs:
    connectionType: Kubernetes Service Connection
    kubernetesServiceEndpoint: saga-aks
    namespace: default
    command: rollout
    arguments: status deployment/order-api --timeout=300s